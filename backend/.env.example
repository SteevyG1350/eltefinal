# Example SMTP configuration. Copy to .env and fill values.
# Use either SMTP_HOST/SMTP_PORT/SMTP_SECURE or SMTP_SERVICE with SMTP_USER and SMTP_PASS

# Using Gmail (recommended: create an App Password and use it as SMTP_PASS)
SMTP_SERVICE=gmail
SMTP_USER=elitesolutions187@gmail.com
SMTP_PASS=REPLACE_WITH_APP_PASSWORD

# Or using a custom SMTP host
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your@smtp-user
# SMTP_PASS=your_smtp_password
